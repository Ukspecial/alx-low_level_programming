#include <stdio.h>

/**
 * main - Enty point
 *
 * Return: Always 0 (Success)
 */


void print_alphabet(void);

int main(void)
{
	int x;

	for (x = a; x <= z; a++)
	{
		putchar(x);
		putchar(\n);
	return (0);
